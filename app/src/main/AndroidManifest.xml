<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.proyecto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.evaluation.Evaluation"></activity>
        <activity android:name=".ui.Theme.DetailTheme" />
        <activity android:name=".ui.changepassword.changePassword" />
        <activity android:name=".ui.editaccount.editAccount" />
        <activity android:name=".ui.RecoverPass.RecoverPass" />
        <activity android:name=".ui.Register.Register" />
        <activity android:name=".ui.login.Login" />
        <activity
            android:name=".ui.Splash.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>